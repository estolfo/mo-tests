#A. Write
#         Setup:
#          - Servers:
#             - serverA: standalone
#          - Op: Insert
#
#1. Description: standalone available
#               (mo step) server started
#               * Op succeeds

description: "Successful read from standalone"

type: "standalone"

init_config: { procParams: { journal: true } }

uriOptions: {}

setup: {
          operations: [
                       {
                         action: "insertOne",
                         doc: { x: 1 }
                        }
                      ]
        }

phases: [
          {
            tests: [
                    {
                      operation: "findOne",
                      outcome: { ok: 1 }
                    }
                  ]
          }
        ]
